import React from "react";
import { Api } from "../../../api";

export const MainPage: React.FC = React.memo(() => {
  return (
    <>
      MAIN
      <button onClick={async () => {}}>LOGIN</button>
    </>
  );
});
